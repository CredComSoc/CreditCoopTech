describe('Example test suite', () => {
    test('must work', () => {
      expect(42).toBe(42)
    })
    test('must work', () => {
        expect(42).toBe(42)
      })
      test('must work2', () => {
        expect(42).toBe(42)
      })
      test('must wor3k', () => {
        expect(42).toBe(42)
      })
      test('must 4work', () => {
        expect(42).toBe(42)
      })
      test('must5 work', () => {
        expect(42).toBe(42)
      })
      test('must6 work', () => {
        expect(42).toBe(42)
      })
      test('must 7work', () => {
        expect(42).toBe(42)
      })
  })